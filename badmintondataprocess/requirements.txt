# Compatibility entry point. The canonical installer is ../setup_runtime.ps1,
# which also installs RTMLib with --no-deps and runs strict GPU verification.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
torchvision==0.26.0+cu128
-r requirements-runtime.txt
